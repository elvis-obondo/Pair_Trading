# Runtime deps for research/*.py and the visualizer/ Streamlit app.
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
statsmodels==0.14.6
streamlit>=1.38
plotly>=5.24

# Only needed for the (currently broken, see CODEBASE_AUDIT.md) Nautilus
# backtest path and the standalone get_data.py explorer — not installed here
# to keep the visualizer's deploy build light. Install separately for local
# full-pipeline work:
#   pip install ccxt nautilus_trader==1.228.0
